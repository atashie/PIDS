# Decision Log

Dated record of major decisions (Guardrail #4). Newest at the bottom. Each large addition/alteration to the repo should add a row.

| Date | Decision | Context / rationale |
|------|----------|---------------------|
| 2026-06-01 | Adopt a **two-axis architecture** — `technologies/` (science) and `applications/` (use cases), joined by an explicit `integration/` layer. | Technologies are reusable building blocks; use cases compose them differently. Keeps concerns independent and makes holistic review tractable. |
| 2026-06-01 | Define **three technology pillars**: PIDS physical system, infiltration-runoff model, aerial mapping — as a **Sense → Solve → Build** pipeline. | Mapping (sense) feeds the model (solve), which designs the physical system (build); field results validate the model. |
| 2026-06-01 | Repository is **private, minimal disclosure process**. | Planning phase; revisit before any public release. |
| 2026-06-01 | Adversarial review implemented as **both documentation discipline and agent-assisted** checks. | Strongest integrity guarantee for third-party and derived numbers. |
| 2026-06-01 | PIDS app. **18/632,186 is in allowance**; prosecution defense is **out of scope**. Patent-strengthening science is **lower priority**, retained for credibility & future filings. | The examiner's rejections were overcome; no live prosecution fight. |
| 2026-06-01 | Pillar 1 folder named **`pids-physical-system/`**. | Disambiguates the physical-system pillar from the repo/company umbrella (also "PIDS"). |
| 2026-06-01 | Build cadence: **chunk by chunk**, with a review gate after each chunk. | Matches the cautious-building guardrail. |
| 2026-06-01 | Applications refined: **eliminating off-site discharge** (vertical infiltration to the deep subsurface) is a **cross-cutting value proposition**; agriculture targets **managed aquifer recharge (MAR)**; suburban gains a **drought-amelioration** co-benefit. | Sharpens the value proposition and the model's objective functions. |
| 2026-06-01 | **Chunk C left intentionally under-specified** — `applications/` (4 use-case folders) and `integration/` scaffolded with high-level READMEs only; detailed design (per-application specs, dependency matrix, interfaces) deferred. | Per user direction; revisit later. |
| 2026-06-04 | **Pillar 2 forward-model engine: BUILD a modular PETSc-FEM model on DOLFINx/FEniCSx (Python), rather than extend an off-the-shelf engine.** The reverse PIDS catch-valve is realized as an implicit variational-inequality / interior-facet residual term. **This revises the earlier "extend a pre-existing model" framing of Pillar 2.** Conditional on a DOLFINx install + VI smoke spike before the architecture is committed. | Full evaluation + de-risk (decision memo [2026-06-03](../docs/plans/2026-06-03-pids-forward-model-engine-evaluation.md)): no single existing engine meets coupled 3-D Richards+overland **and** local sub-meter refinement **and** the *inverse* catch-valve together. A standalone probe proved the catch-valve is tractable only as an **implicit in-solver hook** (operator-split blows up), best realized in an FEM weak form. HydroGeoSphere retained as the validation benchmark; ParFlow eliminated (symmetric barriers / operator-split only). |
| 2026-06-04 | **Forward-model development methodology:** strictly **modular**, built **bottom-up** (subsurface Richards → overland → coupling → PIDS internal-BC layer → domain/forcing/vegetation), with every module sanity-checked **independently and in concert** at every step via a standardized **three-tier routine** (analytical/conservation/plausibility pytest → synthetic typical + 100-yr-extreme forcing → interactive-HTML human inspection). Runtime: DOLFINx/PETSc on **WSL2 + conda**. | Per user direction. Standards: [`claude-sanity-check-routine.md`](claude-sanity-check-routine.md), [`visualize-sanity-check-routine.md`](visualize-sanity-check-routine.md). |
| 2026-06-05 | **Module 2 (overland) depth-positivity: REVISES the locked "SNES-VI (`vinewtonrsls`) reserved for overland `d≥0`" decision** (design §C.4/§H.2). Enforce `d≥0` via a **smooth `newtonls` (back-tracking) headline solve + a post-step CONSERVATIVE positivity limiter** (clip negatives to 0, rescale remaining positive water by `oldtotal/posvol` so the lumped water budget is preserved to machine precision). VI is **retired for overland** (standalone and in the §D monolith). | Adversarial Tier-1 testing (Arik signed off the approach 2026-06-05): `vinewtonrsls` (raw PETSc **and** via DOLFINx 0.10 `NonlinearProblem`, all line searches; `vinewtonssls` too) **cannot robustly solve the stiff, nonsymmetric diffusion-wave system** — it walls within 3–15 steps and violates conservation when the bound binds. The install-spike's VI success was on a *linear symmetric* system and does not transfer. Plain `newtonls` is robust + conserves to ~1e-15 but undershoots `d` by ~1–3 cm at wet/dry fronts; the conservative limiter removes those undershoots while preserving the budget. Implication for §D: the overland block no longer needs a VI path. |
| 2026-06-05 | **Module 3 (coupling) realization confirmed = design-intended top-facet SUBMESH ("S"):** `d` on a codim-1 top-facet submesh, the land-surface exchange `q_ls = k_ex·(ψ_top − d)` integrated on the **parent `ds_top`** (NOT the submesh `dx`), assembled via **low-level blocked assembly + a blocked Newton + `entity_maps`** (the high-level `NonlinearProblem([F0,F1])` cannot host a two-mesh residual block). Exact Galerkin sign-paired coupling (conservation structural), DOF-efficient (`d` thin), `ufl.grad` on the submesh manifold = tangential gradient so M2's overland builder is reused unchanged. Fallbacks documented: **A** (co-locate `d` on the host — simplest but measured ~2× DOFs/memory/solve) and **B** (standalone surface mesh + node-matched **lumped** coupling — DOF-efficient but approximate + hand-rolled). Build 1-D coupled first (realization-agnostic), then 2-D/3-D via S. | M0 R1 spike (`scratch/m3_crossmesh_*`, `m3_spike_findings.md`): integrating the coupling on the submesh `dx` trips FFCX `codim = 1−2 = −1` (can't tabulate a volume field on a facet domain). **Independent Codex review DISSENTED from my initial "submesh not buildable" call and was correct** — the DOLFINx 0.10 HDG-demo idiom couples on the parent `ds_top` (volume field at its natural codim-1 facet trace, submesh fields at codim-0). `probe4` confirms S end-to-end: exact cross-block Jacobian (1 Newton iter at moderate `k_ex`), `k_ex→∞ ⇒ ψ_top=d` continuity collapsing to ~0, conservation exact to machine zero, `d` 13 DOFs vs ψ 169. Residual-extraction refactor landed (commit `2e088ea`, suite 39/39). |
| 2026-06-05 | **Module 3 land-surface exchange: REFINE design §D.3 (smooth Robin) → SUPPLY-LIMITED flux COMPLEMENTARITY (NCP); correct §D.5 (coupled `d≥0` is NOT enforced by VI nor by the M2 limiter).** The actual infiltration flux `λ` is the single sign-paired unknown; a smooth Fischer-Burmeister NCP `Φ(d, τ_c·(q_pot−λ))=0` selects the regime — ponded (`λ=q_pot`, the §D.3 Robin) vs supply-limited (`d=0`, `λ=rain`). Solved with PLAIN Newton (C^∞-smoothed, no VI). Conservation structural (λ sign-paired); reduces to Module-1 `add_ponding_bc` in the no-lateral-flow limit; recovers `ψ_top→d` as `k_ex→∞`. Spec: `docs/plans/2026-06-05-module3-landsurface-ncp-spec.md` (Arik-approved; choices O1 explicit-λ / O2 Fischer-Burmeister / O3 ε-only). | TDD surfaced that the bare Robin §D.3 over-draws: dry soil pulls water from an EMPTY surface store, driving `d<0` (−0.05 m on a closed 1-D column, rain 0.1 < Ks 0.25). Two adversarial reviews (mine + Codex): the M2 post-step limiter CREATES mass for this *systematic* over-draw (vs overland's tiny undershoots); SNES-VI bounding `d≥0` alone is INSUFFICIENT (the soil still over-absorbs `q_pot>supply`, unpaired). The fix is the textbook surface↔subsurface switching BC posed conservatively on the flux. 1-D cycle-1 test GREEN: closed column conserves AND `d≡0` (supply-limited, all rain infiltrates), `ψ_top` stays unsaturated. |
| 2026-06-05 | **Module 3 2-D/3-D realization: BUILD ON A (co-located) NOW; S (submesh, DOF-efficient, design-intended) DEFERRED — blocked by an upstream FFCX 0.10 codegen bug.** A = `d`,`λ` co-located on the host volume (the working 1-D realization), overland as a tangential-gradient surface PDE on host `ds_top`; single mesh, stock toolchain, correct/publishable. S's coupled form set trips an FFCX `build_optimized_tables` bug: a **mixed-dimensional codim-0 `entity_type=="cell"` integral** (the overland self-Jacobian on the submesh) falls through a missing `else` (handles only `facet`/`ridge`) → `t` unbound → `UnboundLocalError`. Reverses the earlier S-over-A choice **for now**; full analysis + the narrow fix in `docs/plans/2026-06-05-module3-realization-ffcx-bug.md`. | M0 spikes + 2 Codex reviews. S compiles in isolation but not in the coupled set (robust vs per-block compile, manifold-first, cache-clear). The fix is a clean narrow branch (`entity_type=="cell"` unpermuted + hard-error) — **probably correct but element-table codegen, so a local patch risks SILENT Jacobian corruption; "matches A" isn't sufficient proof.** Path: build A now (correct on stock FFCX) → file the bug upstream → migrate to S when a fixed release lands (drop-in; physics/NCP/tests are realization-agnostic). A's cost honestly = ~2–3× DOFs / ~2× solve, O(N) (NOT "free after optimization"); tractable for verification, not yet production-cleared. |
