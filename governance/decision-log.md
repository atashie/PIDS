# Decision Log

Dated record of major decisions (Guardrail #4). Newest at the bottom. Each large addition/alteration to the repo should add a row.

| Date | Decision | Context / rationale |
|------|----------|---------------------|
| 2026-06-01 | Adopt a **two-axis architecture** — `technologies/` (science) and `applications/` (use cases), joined by an explicit `integration/` layer. | Technologies are reusable building blocks; use cases compose them differently. Keeps concerns independent and makes holistic review tractable. |
| 2026-06-01 | Define **three technology pillars**: PIDS physical system, infiltration-runoff model, aerial mapping — as a **Sense → Solve → Build** pipeline. | Mapping (sense) feeds the model (solve), which designs the physical system (build); field results validate the model. |
| 2026-06-01 | Repository is **private, minimal disclosure process**. | Planning phase; revisit before any public release. |
| 2026-06-01 | Adversarial review implemented as **both documentation discipline and agent-assisted** checks. | Strongest integrity guarantee for third-party and derived numbers. |
| 2026-06-01 | PIDS app. **18/632,186 is in allowance**; prosecution defense is **out of scope**. Patent-strengthening science is **lower priority**, retained for credibility & future filings. | The examiner's rejections were overcome; no live prosecution fight. |
| 2026-06-01 | Pillar 1 folder named **`pids-physical-system/`**. | Disambiguates the physical-system pillar from the repo/company umbrella (also "PIDS"). |
| 2026-06-01 | Build cadence: **chunk by chunk**, with a review gate after each chunk. | Matches the cautious-building guardrail. |
| 2026-06-01 | Applications refined: **eliminating off-site discharge** (vertical infiltration to the deep subsurface) is a **cross-cutting value proposition**; agriculture targets **managed aquifer recharge (MAR)**; suburban gains a **drought-amelioration** co-benefit. | Sharpens the value proposition and the model's objective functions. |
| 2026-06-01 | **Chunk C left intentionally under-specified** — `applications/` (4 use-case folders) and `integration/` scaffolded with high-level READMEs only; detailed design (per-application specs, dependency matrix, interfaces) deferred. | Per user direction; revisit later. |
| 2026-06-04 | **Pillar 2 forward-model engine: BUILD a modular PETSc-FEM model on DOLFINx/FEniCSx (Python), rather than extend an off-the-shelf engine.** The reverse PIDS catch-valve is realized as an implicit variational-inequality / interior-facet residual term. **This revises the earlier "extend a pre-existing model" framing of Pillar 2.** Conditional on a DOLFINx install + VI smoke spike before the architecture is committed. | Full evaluation + de-risk (decision memo [2026-06-03](../docs/plans/2026-06-03-pids-forward-model-engine-evaluation.md)): no single existing engine meets coupled 3-D Richards+overland **and** local sub-meter refinement **and** the *inverse* catch-valve together. A standalone probe proved the catch-valve is tractable only as an **implicit in-solver hook** (operator-split blows up), best realized in an FEM weak form. HydroGeoSphere retained as the validation benchmark; ParFlow eliminated (symmetric barriers / operator-split only). |
| 2026-06-04 | **Forward-model development methodology:** strictly **modular**, built **bottom-up** (subsurface Richards → overland → coupling → PIDS internal-BC layer → domain/forcing/vegetation), with every module sanity-checked **independently and in concert** at every step via a standardized **three-tier routine** (analytical/conservation/plausibility pytest → synthetic typical + 100-yr-extreme forcing → interactive-HTML human inspection). Runtime: DOLFINx/PETSc on **WSL2 + conda**. | Per user direction. Standards: [`claude-sanity-check-routine.md`](claude-sanity-check-routine.md), [`visualize-sanity-check-routine.md`](visualize-sanity-check-routine.md). |
| 2026-06-05 | **Module 2 (overland) depth-positivity: REVISES the locked "SNES-VI (`vinewtonrsls`) reserved for overland `d≥0`" decision** (design §C.4/§H.2). Enforce `d≥0` via a **smooth `newtonls` (back-tracking) headline solve + a post-step CONSERVATIVE positivity limiter** (clip negatives to 0, rescale remaining positive water by `oldtotal/posvol` so the lumped water budget is preserved to machine precision). VI is **retired for overland** (standalone and in the §D monolith). | Adversarial Tier-1 testing (Arik signed off the approach 2026-06-05): `vinewtonrsls` (raw PETSc **and** via DOLFINx 0.10 `NonlinearProblem`, all line searches; `vinewtonssls` too) **cannot robustly solve the stiff, nonsymmetric diffusion-wave system** — it walls within 3–15 steps and violates conservation when the bound binds. The install-spike's VI success was on a *linear symmetric* system and does not transfer. Plain `newtonls` is robust + conserves to ~1e-15 but undershoots `d` by ~1–3 cm at wet/dry fronts; the conservative limiter removes those undershoots while preserving the budget. Implication for §D: the overland block no longer needs a VI path. |
| 2026-06-05 | **Module 3 (coupling) realization confirmed = design-intended top-facet SUBMESH ("S"):** `d` on a codim-1 top-facet submesh, the land-surface exchange `q_ls = k_ex·(ψ_top − d)` integrated on the **parent `ds_top`** (NOT the submesh `dx`), assembled via **low-level blocked assembly + a blocked Newton + `entity_maps`** (the high-level `NonlinearProblem([F0,F1])` cannot host a two-mesh residual block). Exact Galerkin sign-paired coupling (conservation structural), DOF-efficient (`d` thin), `ufl.grad` on the submesh manifold = tangential gradient so M2's overland builder is reused unchanged. Fallbacks documented: **A** (co-locate `d` on the host — simplest but measured ~2× DOFs/memory/solve) and **B** (standalone surface mesh + node-matched **lumped** coupling — DOF-efficient but approximate + hand-rolled). Build 1-D coupled first (realization-agnostic), then 2-D/3-D via S. | M0 R1 spike (`scratch/m3_crossmesh_*`, `m3_spike_findings.md`): integrating the coupling on the submesh `dx` trips FFCX `codim = 1−2 = −1` (can't tabulate a volume field on a facet domain). **Independent Codex review DISSENTED from my initial "submesh not buildable" call and was correct** — the DOLFINx 0.10 HDG-demo idiom couples on the parent `ds_top` (volume field at its natural codim-1 facet trace, submesh fields at codim-0). `probe4` confirms S end-to-end: exact cross-block Jacobian (1 Newton iter at moderate `k_ex`), `k_ex→∞ ⇒ ψ_top=d` continuity collapsing to ~0, conservation exact to machine zero, `d` 13 DOFs vs ψ 169. Residual-extraction refactor landed (commit `2e088ea`, suite 39/39). |
| 2026-06-05 | **Module 3 land-surface exchange: REFINE design §D.3 (smooth Robin) → SUPPLY-LIMITED flux COMPLEMENTARITY (NCP); correct §D.5 (coupled `d≥0` is NOT enforced by VI nor by the M2 limiter).** The actual infiltration flux `λ` is the single sign-paired unknown; a smooth Fischer-Burmeister NCP `Φ(d, τ_c·(q_pot−λ))=0` selects the regime — ponded (`λ=q_pot`, the §D.3 Robin) vs supply-limited (`d=0`, `λ=rain`). Solved with PLAIN Newton (C^∞-smoothed, no VI). Conservation structural (λ sign-paired); reduces to Module-1 `add_ponding_bc` in the no-lateral-flow limit; recovers `ψ_top→d` as `k_ex→∞`. Spec: `docs/plans/2026-06-05-module3-landsurface-ncp-spec.md` (Arik-approved; choices O1 explicit-λ / O2 Fischer-Burmeister / O3 ε-only). | TDD surfaced that the bare Robin §D.3 over-draws: dry soil pulls water from an EMPTY surface store, driving `d<0` (−0.05 m on a closed 1-D column, rain 0.1 < Ks 0.25). Two adversarial reviews (mine + Codex): the M2 post-step limiter CREATES mass for this *systematic* over-draw (vs overland's tiny undershoots); SNES-VI bounding `d≥0` alone is INSUFFICIENT (the soil still over-absorbs `q_pot>supply`, unpaired). The fix is the textbook surface↔subsurface switching BC posed conservatively on the flux. 1-D cycle-1 test GREEN: closed column conserves AND `d≡0` (supply-limited, all rain infiltrates), `ψ_top` stays unsaturated. |
| 2026-06-05 | **Module 3 2-D/3-D realization: BUILD ON A (co-located) NOW; S (submesh, DOF-efficient, design-intended) DEFERRED — blocked by an upstream FFCX 0.10 codegen bug.** A = `d`,`λ` co-located on the host volume (the working 1-D realization), overland as a tangential-gradient surface PDE on host `ds_top`; single mesh, stock toolchain, correct/publishable. S's coupled form set trips an FFCX `build_optimized_tables` bug: a **mixed-dimensional codim-0 `entity_type=="cell"` integral** (the overland self-Jacobian on the submesh) falls through a missing `else` (handles only `facet`/`ridge`) → `t` unbound → `UnboundLocalError`. Reverses the earlier S-over-A choice **for now**; full analysis + the narrow fix in `docs/plans/2026-06-05-module3-realization-ffcx-bug.md`. | M0 spikes + 2 Codex reviews. S compiles in isolation but not in the coupled set (robust vs per-block compile, manifold-first, cache-clear). The fix is a clean narrow branch (`entity_type=="cell"` unpermuted + hard-error) — **probably correct but element-table codegen, so a local patch risks SILENT Jacobian corruption; "matches A" isn't sufficient proof.** Path: build A now (correct on stock FFCX) → file the bug upstream → migrate to S when a fixed release lands (drop-in; physics/NCP/tests are realization-agnostic). A's cost honestly = ~2–3× DOFs / ~2× solve, O(N) (NOT "free after optimization"); tractable for verification, not yet production-cleared. |
| 2026-06-06 | **Module 3 lateral OUTFLOW boundary condition (realization A): impose the Manning normal-depth POINT discharge at the codim-2 surface outlet via a single-mesh VERTEX (`dP`) integral.** In the co-located realization the surface lives on host `ds_top` (codim-1), so the outlet — the *boundary of the surface* (downstream-TOP corner in 2-D) — is a codim-2 host entity. `add_outflow_bc(locator, slope>0)` mirrors Module-2's API and adds `q_out=86400·(1/n)·d^{5/3}·√slope` at the outlet vertex; `outflow_rate`/`cum_outflow` (recorded pre-limiter) ⇒ `Δtotal=cum_rain−cum_outflow`. Also a **conservation fix to the diagonal-allocation hack** for the pinned interior d/λ rows: the old whole-domain `eps_diag·d·v·dx` is replaced by a `dP` integral on ONLY the pinned interior vertices (shared meshtags with the outlet, tag 1) → conservation now STRUCTURAL. Full suite 55/55. | Spike `scratch/m3_outflow_spike.py`: `dP` assembles the EXACT nodal discharge + finite auto-Jacobian and — single-mesh, not the mixed-dim codim-0 path — does NOT re-trigger the realization-S FFCX bug. **Codex adversarial review (2026-06-06) → 2 must-fixes applied + TDD'd:** (1) the whole-domain `eps_diag` leaked ~1.18e-12 into the free top rows (not "structural") → vertex-pin allocation (5.5e-14, `test_2d_closed_conservation_is_structural`, RED→GREEN); (2) empty-outlet locator now raises (`test_2d_outflow_bc_rejects_empty_outlet`). Deferred (ffcx-bug doc §7): outlet tagging not MPI-safe; 3-D outlet = codim-2 perimeter CURVE needs a `ridge`-integral spike (band-on-`ds_top` fallback); no explicit FD-Jacobian regression. Authoritative: `docs/plans/2026-06-05-module3-realization-ffcx-bug.md` §7. |
| 2026-06-06 | **Module 3 land-surface infiltration: REPLACE the dry cell-value conductance soil leg `K(ψ_top)/ℓ_c·(d−ψ_top)` (design §D.3) with a SORPTIVE Kirchhoff matric-flux-potential leg `q_pot = [∫_{ψ_top}^{d} K(ψ)dψ]/ℓ_c`.** The dry value misses that the surface saturates (K→Ks) across the wetting film, under-infiltrating dry soil ~50× at coarse resolution (benchmark: loam 2% vs 89% resolved; sand ~0% vs 100%) — disqualifying for the PIDS recharge question (Arik 2026-06-06: "without sorptivity the model is fundamentally flawed"). New constitutive primitive `VanGenuchten.kirchhoff(a,b)`/`kirchhoff_ufl` (graded composite-Simpson over [a,b], clustered near the wet end; C¹/smooth — no max kink; saturated ψ>0 via the air-entry cap; n=32/p=4 converged <1% vs dense ref). The NCP structure, λ sign-pairing, lateral overland, outflow, limiter are UNCHANGED; reduces to head continuity as ℓ_c→0. **Scope:** §D land surface only (a quasi-steady coarse-cell fix — the Richards solve below carries the transient); the §E feature soil leg (`Ω_geom·[Φ(ψ_far)−Φ(ψ_wall)]`) is a SEPARATE Module-4 gate (unresolved transient near-field; resolved-annulus reference + transient-clock fallback). | Spike + 4-way benchmark (`scratch/m3_sorptivity_benchmark.py`): the closure RECOVERS at fine mesh (loam 0.96) so §D was an UNDER-RESOLUTION problem, and the Kirchhoff leg recovers it at COARSE mesh (sand ratio 1.00; loam 0.70→0.96; fixes sand fine-mesh convergence) with NO per-face history state. **Two Codex adversarial reviews** (design analysis → corrected the "broken closure" overstatement to under-resolution + the §D/§E split; spec → fixed the non-smooth `max(d,0)` (use the smooth Kirchhoff primitive), kept the structural-conservation guard BEFORE relaxing its gate, split validation into quasi-steady + transient, reframed §E as separate). TDD: `kirchhoff` convergence test; sorptivity-recovery (RED 0.016→GREEN 0.49) + transient-tracking vs resolved Richards; tolerance-free `test_2d_surface_balance_is_structural` (assembled `<F_d,v=1>` over free rows == storage+λ to machine precision — the real leak guard) + relaxed numeric gate (tracks Newton atol). Full suite 60/60. Authoritative: `docs/plans/2026-06-06-sorptivity-exchange-{design-analysis,spec}.md`. |
| 2026-06-07 | **Module 3: ADD a subsurface Darcy/head DRAINAGE BC (general-head / Cauchy / MODFLOW GHB).** Outward Darcy flux `q_n = C·(H − H_ext)`, `H = ψ + z` (z=elevation; convention from the bulk residual `K(∇ψ+e_g)·∇v`, e_g=+ẑ). Lets the SOIL MATRIX exchange water with an external reservoir — lateral groundwater outflow (a side), deep percolation (the base), or a drain — DISTINCT from the surface Manning outlet (open-channel, wrong for the matrix). Closes the "soil matrix is a closed reservoir" gap (the BC/texture sweeps had all subsurface boundaries no-flux). Bidirectional; reduces to no-flow (C→0) / Dirichlet (C→∞); enters `F_psi` as a standard codim-1 facet term `+q_n·v·ds`. `add_drainage_bc(locator, conductance, external_head)` on RichardsProblem + CoupledProblem; `drainage_rate()`/`cum_drainage`; balance `Δtotal = cum_rain − cum_outflow − cum_drainage`. | Spec `docs/plans/2026-06-07-subsurface-drainage-bc-spec.md`. TDD: ANALYTICAL steady Darcy (saturated column, Dirichlet top + GHB base → q = Ks(H_top−H_base)/L = C(H_base−H_ext), 2%); no-flow (C=0) + Dirichlet (large C → boundary head→H_ext) limits; coupled conservation (Δtotal=−cum_drainage, 1e-6); negative-C guard. Required an `F_psi` refactor (`_build_F_psi`): the λ-top and drainage facet integrals share ONE facet meshtags (DOLFINx one-subdomain_data-per-type rule, same as the vertex `_finalize_forms`). Constant-C GHB; the Richards solve self-limits unsaturated drainage to the soil's K-capacity (K(ψ)-weighted conductance, unit-gradient base, seepage-face NCP = noted future refinements). Full suite 64/64. |
| 2026-06-07 | **Module 3 drainage BC: CORRECT the constant-C GHB to RELATIVE-PERMEABILITY-WEIGHTED `q_n = C·kr(ψ)·(H−H_ext)`, kr=K(ψ)/Ks — REVISES the "constant-C / Richards self-limits to K-capacity" claim in the row above (it was WRONG).** A constant-C GHB imposes the flux regardless of saturation, so the solver drives the boundary head down through an unresolved layer to deliver it → OVER-drains unsaturated soil. The kr weight makes it the physical Darcy flux through a boundary film of conductivity K(ψ): self-limits as the boundary dries, reduces to standard GHB when saturated (kr=1). Also: disjointness guard in `add_drainage_bc`/`_build_F_psi` (no double-tagged facets); the FULL balance carries `+ clip_mass_adjust`; `add_drainage_bc` now accepts a **fem.Constant conductance (time-varying / rampable)** and returns the handle. **Plus the lateral-drainage 2-D ASSESSMENT (saturating storm + C/H_ext sweep):** the original Hortonian texture-sweep storms never saturate the soil MATRIX (their runoff is surface ponding → Manning outlet) so the lateral drain stays idle; a SATURATION-EXCESS scenario (antecedent water table z=0.7, sustained rain < every Ks) exercises it. Texture-dependent value-prop: loam = clean win (surface runoff→0, drained laterally); sand de-waters the antecedent water table; silt partial; clay modest. All 24 cases conserve (independently re-extracted, rel resid ≤ 4.4e-10). Full suite 68/68. | **Two Codex adversarial reviews.** (1) Pre-assessment code/physics review → "fix-then-assess": no sign bug, but constant-C over-drains unsaturated soil (gradients q/K ≈ 12–92 for loam) → the kr-weighting fix + overlap guard + clip-balance, all TDD'd (`test_drainage_relative_permeability_weighting`: unsaturated drains 57× too fast without kr; saturated analytical gate unchanged at kr=1). (2) Post-assessment review → "FIX-FIRST" (results sound, framing overclaimed): relabel "infiltrated" → net soil-water STORAGE change (negative for sand = draining stored groundwater, NOT negative infiltration); the drain-conductance RAMP (0→C over 0.02 d, added to fix a cold-start divergence on stiff near-saturated clay/silt) is a numerical regularization to report, not hide; don't call clay's residual Hortonian (the scenario is saturation-excess); the bidirectional INJECTION branch was untested by the all-outflow sweep → pinned by `test_drainage_bc_bidirectional_inflow`; sat%/water-table are coarse mid-slope proxies (HTML water-table now the interpolated ψ=0 crossing, not node-snapped). Generators `viz/run_lateral_{drainage,saturating}_sweep.py` + `viz/make_lateral_saturating_html.py`; spec §6. Perf gotcha: pin `*_NUM_THREADS=1` (multithreaded BLAS oversubscribes the tiny systems ~30–50×). |
| 2026-06-08 | **Module 3 EXTENDED to 3-D (realization A).** The coupled `[ψ,d,λ]` system is dimension-agnostic except the lateral-outflow OUTLET: in 3-D it is a codim-2 perimeter EDGE imposed as a single-mesh **ridge (`dr`) LINE integral** `∫_edge q_out·v ds = (per-width Manning q_out)·edge_length` (path R; the normalized band-on-`ds_top` sink is the documented fallback). `add_outflow_bc`/`_finalize_forms` gained a dimension branch (the vertex pin and the 3-D ridge outlet are DIFFERENT integral types → separate meshtags, FFCX-safe) + an outlet-vs-outlet overlap guard. **Plus a PERF fix — a `quadrature_degree` CAP (default 8)** on the van-Genuchten/Manning/Kirchhoff integrands: FFCX auto-estimates ~26 (Jacobian ~41), O(deg³) on TETS → ~1000× slower assembly (a 56-min smoke → seconds; per-step 16.5 s→0.08 s), **bit-identical at smooth states**; `richards_bulk_residual` default `None` keeps auto so **Module 1 / MMS is untouched**. Full suite **85/85**; **Tier-3 signed off (Arik 2026-06-08).** | Plan `docs/plans/2026-06-07-module3-3d-plan.md`; report `validation/sanity/coupling_3d__2026-06-08.md`; memory `pids-fem-quadrature-degree-cap`. Phase-0 ridge spike + an **S-style coexistence guard** proved `ufl.Measure("ridge")` is FFCX-native on stock 0.10 (exact line discharge 3.8e-16, finite auto-Jacobian) and does NOT re-trigger the realization-S mixed-dim codim-0 bug (the `ridge` branch is fully implemented; A is single-mesh → `is_mixed_dim=False`). **TWO independent multi-agent adversarial reviews** (spike → unanimous adopt-R; full impl → fix-then-ship, no blockers): should-fixes applied + TDD'd (overlap guard, FD-Jacobian incl. the d=0 kink, 2-outlet, the cap-accuracy CONTRACT test since conservation is structural and can't catch Darcy under-integration, reduces-to-2-D, `gdim==tdim` assert, 1-D outflow guard; the quadrature claim was CORRECTED from an overstated "bit-identical from degree 2" to smooth-only + a documented air-entry-kink deviation that is end-to-end benign). Carried: MPI serial-only (ridge tagging worse than vertices — edges straddle partitions); flat-top + axis-aligned outlet envelope; coarse-mesh surface non-uniformity is CONVERGENT (λ std 1.9e-2→1.6e-8 under refinement, the 3-D analog of concern #9). Tier-3: a SAND (deep infiltration + lateral groundwater) vs LOAM (infiltration-excess overland) texture contrast on a 3-D hillslope, identical storm. |
